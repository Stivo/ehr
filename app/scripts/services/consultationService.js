'use strict';

var app = angular.module('ehrApp');

app.factory('ConsultationService', function () {
    var consultationService = {};

    consultationService.createConsultation = function () {
    };

    return consultationService;

});
